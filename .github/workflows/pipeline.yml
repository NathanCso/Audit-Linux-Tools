name: Pipeline automatisé pour notre outil d'audit 

on:
  push:
    branches:
      - main  
  pull_request:
    branches:
      - main  

jobs:
  build:
    runs-on: debian-latest  

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3  

    - name: Set up Docker
      uses: docker/setup-buildx-action@v2  

    - name: Build Docker image
      run: |
        docker build -t system-audit-image .  

    - name: Run tests (optional)
      run: |
        

    - name: Push Docker image to Docker Hub (optional)
      if: github.ref == 'refs/heads/main'  
      run: |
        docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
        docker tag system-audit-image your_dockerhub_username/system-audit-image:latest
        docker push your_dockerhub_username/system-audit-image:latest

    - name: Deploy (optional)
      run: |
        
